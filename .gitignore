node_modules/
dist/
build/
.next/
.expo/
.expo-shared/
coverage/

# Env files — NO se versionan. Cada entorno (local / VPS) tiene su
# propio .env con credenciales (PayPhone, SMTP, JWT secrets, etc).
.env
.env.local
.env.*.local
.env.production
.env.development
# Pero sí dejamos los .env.example como plantilla pública.
!.env.example

*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

.DS_Store
Thumbs.db

.vscode/
.idea/
*.swp
*.swo

prisma/migrations/dev.db*

# Vite escribe estos archivos temporales al cargar config con TS — no
# deben versionarse (cambian en cada arranque del dev server).
vite.config.ts.timestamp-*.mjs

.claude/
